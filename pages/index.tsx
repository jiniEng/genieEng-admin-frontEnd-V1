import type { NextPage } from "next";
import MainHeader from "../components/common/header/mainHeader";

const Home: NextPage = () => {
  return (
    <div>
      <MainHeader />
    </div>
  );
};

export default Home;
